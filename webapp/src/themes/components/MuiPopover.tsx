import { MuiComponentThemeDefinition } from "../utils";
import { PopoverClassKey, PopoverProps } from "@mui/material";

export const MuiPopover: MuiComponentThemeDefinition<
  PopoverClassKey,
  PopoverProps
> = {
  props: {},
  override: () => ({}),
};
