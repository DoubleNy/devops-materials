import { MuiComponentThemeDefinition } from "../utils";
import { MenuItemClassKey, MenuItemProps } from "@mui/material";

export const MuiMenuItem: MuiComponentThemeDefinition<
  MenuItemClassKey,
  MenuItemProps
> = {
  props: {},
  override: () => ({}),
};
