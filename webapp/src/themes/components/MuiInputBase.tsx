import { MuiComponentThemeDefinition } from "../utils";
import { InputBaseClassKey, InputBaseProps } from "@mui/material";

export const MuiInputBase: MuiComponentThemeDefinition<
  InputBaseClassKey,
  InputBaseProps
> = {
  props: {},
  override: () => ({}),
};
