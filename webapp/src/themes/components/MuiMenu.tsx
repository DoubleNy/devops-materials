import { MuiComponentThemeDefinition } from "../utils";
import { LIGHT_BLUE } from "./utils";
import { MenuClassKey, MenuProps } from "@mui/material";

export const MuiMenu: MuiComponentThemeDefinition<MenuClassKey, MenuProps> = {
  props: {},
  override: () => ({}),
};
