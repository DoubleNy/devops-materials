import { MuiComponentThemeDefinition } from "../utils";
import { BLUE, YELLOW } from "./utils";
import { ListItemClassKey, ListItemProps } from "@mui/material";

export const MuiListItem: MuiComponentThemeDefinition<
  ListItemClassKey,
  ListItemProps
> = {
  props: {},
  override: () => ({}),
};
