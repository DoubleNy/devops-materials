import { MuiComponentThemeDefinition } from "../utils";
import { TypographyClassKey, TypographyProps } from "@mui/material";

export const MuiTypography: MuiComponentThemeDefinition<
  TypographyClassKey,
  TypographyProps
> = {
  props: {},
  override: () => ({}),
};
